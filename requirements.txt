anthropic==0.40.0
telethon==1.37.0
# Pinned at 3.13.1: the Bot API 9.4 inline-button `style` field landed in
# aiogram 3.23.0+, which requires Python >=3.10,<3.15. This interpreter is
# 3.9, so upgrading is infeasible. We pass `style` through instead — aiogram's
# InlineKeyboardButton model is `extra='allow'`, so the field is accepted and
# serialised into the Bot API JSON (see job_hunter/bot.py::_build_button).
aiogram==3.13.1
python-dotenv==1.0.1
PyYAML==6.0.2
# Daily 10:00-local harvest scheduler shared with serve's event loop
# (AsyncIOScheduler). 3.11.x is the last 3.x line and supports Python 3.9.
# Pulls in tzlocal for local-tz detection.
APScheduler==3.11.2
# IANA timezone database for stdlib zoneinfo. The python:3.9-slim base image has
# no system tzdata, so ZoneInfo("Europe/Belgrade") would raise without this.
tzdata==2026.2
httpx==0.27.2
# PostgreSQL driver: synchronous psycopg v3. The [binary] extra installs the
# precompiled libpq wheel (manylinux), so no system libpq / build toolchain is
# needed in the slim image.
psycopg[binary]==3.2.9

# --- dev / test only --------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
# Ephemeral PostgreSQL for the test suite. With the postgresql_noproc fixture it
# connects to an externally-provided Postgres (a docker postgres:16 container on
# this machine) and creates a fresh template-cloned DB per test (function scope),
# preserving the per-test isolation the old in-memory SQLite gave.
pytest-postgresql==6.1.1
